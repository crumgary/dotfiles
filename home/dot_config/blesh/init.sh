# ~/.config/blesh/init.sh — ble.sh user config. Sourced automatically by ble.sh on startup.
#
# Empty by default. Add ble-bind / bleopt lines here for personal tweaks.
#
# Default auto-complete keys (when ghost text is shown):
#   Right-arrow / End / Ctrl-E   accept the full suggestion
#   Alt-F                         accept one word
#
# To experiment with Tab-to-accept (conflicts with normal tab-completion when
# no suggestion is showing), uncomment the next line:
#   ble-bind -m auto_complete -f 'TAB' auto_complete/insert
